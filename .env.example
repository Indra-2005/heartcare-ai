# HeartCare AI – Environment Configuration

# Flask Secret Key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your_very_secret_key_here_change_this

# PostgreSQL Database URL
DATABASE_URL=postgresql://username:password@localhost:5432/heartcare_db

# Optional: Debug mode (set to False in production)
# FLASK_DEBUG=False
